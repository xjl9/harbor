// Phone shell strings for account surfaces; the i18n coverage test requires every
// translated locale to carry every t() key used by the app.
const mobileAccount: Record<string, string> = {
};

export default mobileAccount;
