// Phone shell strings for detail surfaces; the i18n coverage test requires every
// translated locale to carry every t() key used by the app.
const mobileDetail: Record<string, string> = {
};

export default mobileDetail;
