<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN" "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
<plist version="1.0">
<dict>
	<key>com.apple.security.cs.allow-unsigned-executable-memory</key>
	<true/>
	<key>com.apple.security.cs.disable-library-validation</key>
	<true/>
	<!-- Lets raw mDNS multicast (UDP 5353) work for LAN auto-discovery. This is a
	     managed entitlement Apple normally grants via an approved provisioning
	     profile; on a sideload build without that profile it is harmless but may
	     not be honored, so discovery can come back empty. The manual-IP connect
	     path is kept as a fallback for exactly that case. -->
	<key>com.apple.developer.networking.multicast</key>
	<true/>
</dict>
</plist>
